//update the api event handler for gathering data from the api link and format the required ones
//into a method's return.
import 'main.dart';
class api_handler extends MyApp{
  api_handler({super.key});

  String batteryLevel(){
    String link = "link of api";
//format the data from the api
    return link;
  }
}